{
  "name": "extplug-autowoot",
  "version": "2.0.0",
  "description": "ExtPlug plugin that woots every song automatically.",
  "main": "build/autowoot.js",
  "repository": "extplug/autowoot",
  "keywords": [
    "extplug-plugin",
    "plug.dj",
    "autowoot"
  ],
  "files": [
    "build",
    "src"
  ],
  "devDependencies": {
    "extplug-cli": "^1.0.0"
  },
  "scripts": {
    "prepublishOnly": "npm run build -- --minify",
    "build": "extplug bundle src/main.js build/autowoot.js"
  }
}
